class Node{ // criando a classe nó
    constructor(element){ // criando o construtor da classe nó
        this.content = element; // criando a variavel conteudo para receber o elemento
        this.next = null; // criando a variavel next para receber nulo inicialmente
    }
}